import { connect } from 'react-redux';
import UIPractice from './UIPractice';

const mapStateToProps = function(){
  return{};
}

const mapDispatchToProps = function(){
  return{};
}

const UIPracticeCont = connect(mapStateToProps,mapDispatchToProps)(UIPractice);

export default UIPracticeCont;